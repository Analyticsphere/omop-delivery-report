% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_calculators.R
\name{calculate_harmonization}
\alias{calculate_harmonization}
\title{Calculate vocabulary harmonization impact}
\usage{
calculate_harmonization(same_table_rows, valid_rows, transitions_in)
}
\arguments{
\item{same_table_rows}{Integer rows after same-table mappings}

\item{valid_rows}{Integer valid input rows}

\item{transitions_in}{Integer rows received from other tables}
}
\value{
Integer harmonization impact (negative = rows lost, positive = rows gained)
}
\description{
Formula: (same_table_result_rows - valid_rows) + transitions_in
- same_table_result_rows: Rows remaining after same-table vocab mappings (includes 1:N expansion)
- valid_rows: Valid input rows (baseline, excludes quality issues)
- transitions_in: Rows received from other tables via cross-table mappings
}
\examples{
calculate_harmonization(95, 100, 0)    # -5 (net loss)
calculate_harmonization(105, 100, 10)  # 15 (net gain)
calculate_harmonization(100, 100, 0)   # 0 (balanced)
}
